; Online Sale allows up to four images of 2 MiB each. Keep PHP above the
; Laravel per-file limit and align the aggregate request limit with Nginx.
upload_max_filesize = 8M
post_max_size = 25M
max_file_uploads = 20
